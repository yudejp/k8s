apiVersion: v1
kind: Service
metadata:
  name: growi
  namespace: growi
spec:
  type: ClusterIP
  selector:
    app: growi
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
