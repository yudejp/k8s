apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
- deployment.yaml
- service.yaml
- secret.yaml

labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/name: keikenchi-map
    app.kubernetes.io/part-of: keikenchi-map
