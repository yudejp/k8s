apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml
- secret.yaml

labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/name: swarm-checkin
    app.kubernetes.io/part-of: swarm-checkin
