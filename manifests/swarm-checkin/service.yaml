apiVersion: v1
kind: Service
metadata:
  name: swarm-checkin
  labels:
    app: swarm-checkin
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: swarm-checkin
