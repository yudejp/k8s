apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nrt1-elasticsearch-es
  namespace: default
spec:
  defaultBackend:
    service:
      name: elasticsearch-es-http
      port:
        number: 9200
  ingressClassName: tailscale
  tls:
    - hosts:
        - nrt1-elasticsearch-es