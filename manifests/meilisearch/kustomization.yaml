apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - secret.yaml
  - ingress.yaml

configMapGenerator:
  - name: meilisearch-migration
    files:
      - migration.sh
