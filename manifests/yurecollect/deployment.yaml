apiVersion: apps/v1
kind: Deployment
metadata:
  name: yurecollect
spec:
  replicas: 1
  selector:
    matchLabels:
      app: yurecollect
  template:
    metadata:
      labels:
        app: yurecollect
    spec:
      containers:
        - name: yurecollect
          image: ghcr.io/yude/yurecollect:sha-18483c8
          args:
            - "wss://unstable.kusaremkn.com/yure/"
          resources:
            requests:
              memory: "512Mi"
              cpu: "10m"
            limits:
              memory: "1280Mi"
              cpu: "100m"
          ports:
            - containerPort: 3000
