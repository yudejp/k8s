apiVersion: v1
kind: Service
metadata:
  name: growi
  namespace: growi-int
spec:
  type: ClusterIP
  selector:
    app: growi
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
