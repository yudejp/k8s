apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nrt1-pgadmin
  namespace: default
spec:
  defaultBackend:
    service:
      name: pgadmin-service
      port:
        number: 80
  ingressClassName: tailscale
  tls:
    - hosts:
        - nrt1-pgadmin
