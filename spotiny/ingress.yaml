apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: spotiny
  annotations:
    tailscale.com/funnel: "true"
spec:
  defaultBackend:
    service:
      name: spotiny
      port:
        number: 80
  ingressClassName: tailscale
  tls:
    - hosts:
        - spotiny
